<script src="./socket.io/socket.io.js"></script>
<script>
             var socket = io()

        let  clientid, infor
        
        socket.on('vps-send-clientID',(id)=>{
            clientid = id.clientID 
            socket.emit('client-send-OS_index', clientid, 1)
            console.log(clientid)
        })

        socket.on('vps-send-data', (data)=>{
            console.log(data.data[1])
                for (i=0; i<12; i++){
                    const indicator = [];
                    indicator[i] = data.data[i].indicator
                    $("#"+ indicator[i]).html(data.data[i].value)
                } 
        })
        
        $(document).ready(function(){
        });

</script>
    <div id="Flow In" style="display: none;"></div>
    <div id="Flow out" style="display: none;"></div>
    <div id="Temperature" style="display: none;"></div>
    <div id="Color" style="display: none;"></div>
    <div id="pH" style="display: none;"></div>
    <div id="TSS" style="display: none;"></div>
    <div id="COD" style="display: none;"></div>
    <div id="NH4+" style="display: none;"></div>
    <div id="Photpho+" style="display: none;"></div>
    <div id="Nito" style="display: none;"></div>
    <div id="TOC" style="display: none;"></div>
    <div id="Clo" style="display: none;"></div>


<div class="list-group" style="float: left; width: 10%">
  <a href="#" class="list-group-item list-group-item-action active">
    Stations list
  </a>
  <a href="/trend" class="list-group-item list-group-item-action">
      <i class="fa-solid fa-water"></i>
      Tram 1 Bach Khoa </a>
  <a href="/tram1" class="list-group-item list-group-item-action">
      <i class="fa-solid fa-house-flood-water"></i>
      Tram 2 <b>Bach Khoa</b></a>
  <a href="/tram2" class="list-group-item list-group-item-action">
      <i class="fa-solid fa-wind"></i>
      Tram 3 Bach Khoa</a>
  <a href="/tram3" class="list-group-item list-group-item-action">
      <i class="fa-solid fa-industry"></i>
      Tram 4 Bach Khoa</a>

</div>

<div style="float: right; width: 100%" >
    <h3 style="text-align: center; font-size: xx-large;" >Tram Bach Khoa</h3>
    <div id="div1" style="float: left; width: 50%; height: 50%;">
        <div style="font-size: x-large; text-align: center;" id="text1">TOC</div>
        <canvas id="myChart" style="width:100%;max-width:700px"></canvas> 
               <script>
            new Chart("myChart", {
            type: "line",
            data: {
                datasets: [{
                    label: "mg/L",
                    backgroundColor: "rgba(0,0,0,1.0)",
                    borderColor: "rgba(0,0,0,0.1)",
                    data: []
                }]
            },
            options: {
                legend: {display: false},
                scales: {
                    x: {
                        type: 'realtime',   // x axis will auto-scroll from right to left
                        realtime: {         // per-axis options
                        duration: 20000,  // data in the past 20000 ms will be displayed
                        refresh: 1000,    // onRefresh callback will be called every 1000 ms
                        delay: 1000,      // delay of 1000 ms, so upcoming values are known before plotting a line
                        pause: false,     // chart is not paused
                        ttl: undefined,   // data will be automatically deleted as it disappears off the chart
                        frameRate: 30,    // data points are drawn 30 times every second
                        
                        // a callback to update datasets
                        onRefresh: chart => {
                            chart.data.datasets.forEach(dataset => {
                                    dataset.data.push({
                                    x: Date.now(),
                                    y: document.getElementById("TOC").innerHTML
                                })
                            });
                        }
                        }
                    },
                }
            }
            });
    
        </script>
 
    </div>
    <div id="div2" style="float: right; width: 50%; height: 50%;">
        <div style="font-size: x-large; text-align: center;" id="text1">Nito</div>
        <canvas id="myChart2" style="width:100%;max-width:700px"></canvas> 
               <script>
            new Chart("myChart2", {
            type: "line",
            data: {
                datasets: [{
                    label: "mg/L",
                    backgroundColor: "rgba(0,0,0,1.0)",
                    borderColor: "rgba(0,0,0,0.1)",
                    data: []
                }]
            },
            options: {
                legend: {display: false},
                scales: {
                    x: {
                        type: 'realtime',   // x axis will auto-scroll from right to left
                        realtime: {         // per-axis options
                        duration: 20000,  // data in the past 20000 ms will be displayed
                        refresh: 1000,    // onRefresh callback will be called every 1000 ms
                        delay: 1000,      // delay of 1000 ms, so upcoming values are known before plotting a line
                        pause: false,     // chart is not paused
                        ttl: undefined,   // data will be automatically deleted as it disappears off the chart
                        frameRate: 30,    // data points are drawn 30 times every second
                        
                        // a callback to update datasets
                        onRefresh: chart => {
                            chart.data.datasets.forEach(dataset => {
                                    dataset.data.push({
                                    x: Date.now(),
                                    y: document.getElementById("Nito").innerHTML
                                })
                            });
                        }
                        }
                    },
                }
            }
            });
    
        </script>
 
    </div>
    <div id="div3" style="float: left; width: 50%; height: 50%;">
        <div style="font-size: x-large; text-align: center;" id="text1">Temperature</div>
        <canvas id="myChart3" style="width:100%;max-width:700px"></canvas> 
               <script>
            new Chart("myChart3", {
            type: "line",
            data: {
                datasets: [{
                    label: "ºC",
                    backgroundColor: "rgba(0,0,0,1.0)",
                    borderColor: "rgba(0,0,0,0.1)",
                    data: []
                }]
            },
            options: {
                legend: {display: false},
                scales: {
                    x: {
                        type: 'realtime',   // x axis will auto-scroll from right to left
                        realtime: {         // per-axis options
                        duration: 20000,  // data in the past 20000 ms will be displayed
                        refresh: 1000,    // onRefresh callback will be called every 1000 ms
                        delay: 1000,      // delay of 1000 ms, so upcoming values are known before plotting a line
                        pause: false,     // chart is not paused
                        ttl: undefined,   // data will be automatically deleted as it disappears off the chart
                        frameRate: 30,    // data points are drawn 30 times every second
                        
                        // a callback to update datasets
                        onRefresh: chart => {
                            chart.data.datasets.forEach(dataset => {
                                    dataset.data.push({
                                    x: Date.now(),
                                    y: document.getElementById("Temperature").innerHTML
                                })
                            });
                        }
                        }
                    },
                }
            }
            });
     
        </script>
 
    </div>
    <div id="div4" style="float: right; width: 50%; height: 50%;">
        <div style="font-size: x-large; text-align: center;" id="text1">Color</div>
        <canvas id="myChart4" style="width:100%;max-width:700px"></canvas> 
               <script>
            new Chart("myChart4", {
            type: "line",
            data: {
                datasets: [{
                    label: "mg/L",
                    backgroundColor: "rgba(0,0,0,1.0)",
                    borderColor: "rgba(0,0,0,0.1)",
                    data: []
                }]
            },
            options: {
                legend: {display: false},
                scales: {
                    x: {
                        type: 'realtime',   // x axis will auto-scroll from right to left
                        realtime: {         // per-axis options
                        duration: 20000,  // data in the past 20000 ms will be displayed
                        refresh: 1000,    // onRefresh callback will be called every 1000 ms
                        delay: 1000,      // delay of 1000 ms, so upcoming values are known before plotting a line
                        pause: false,     // chart is not paused
                        ttl: undefined,   // data will be automatically deleted as it disappears off the chart
                        frameRate: 30,    // data points are drawn 30 times every second
                        
                        // a callback to update datasets
                        onRefresh: chart => {
                            chart.data.datasets.forEach(dataset => {
                                    dataset.data.push({
                                    x: Date.now(),
                                    y: document.getElementById("Color").innerHTML
                                })
                            });
                        }
                        }
                    },
                }
            }
            });
    
        </script>
 
    </div>
    <div id="div5" style="float: left; width: 50%; height: 50%;">
        <div style="font-size: x-large; text-align: center;" id="text1">pH</div>
        <canvas id="myChart5" style="width:100%;max-width:700px"></canvas> 
               <script>
            new Chart("myChart5", {
            type: "line",
            data: {
                datasets: [{
                    label: "",
                    backgroundColor: "rgba(0,0,0,1.0)",
                    borderColor: "rgba(0,0,0,0.1)",
                    data: []
                }]
            },
            options: {
                legend: {display: false},
                scales: {
                    x: {
                        type: 'realtime',   // x axis will auto-scroll from right to left
                        realtime: {         // per-axis options
                        duration: 20000,  // data in the past 20000 ms will be displayed
                        refresh: 1000,    // onRefresh callback will be called every 1000 ms
                        delay: 1000,      // delay of 1000 ms, so upcoming values are known before plotting a line
                        pause: false,     // chart is not paused
                        ttl: undefined,   // data will be automatically deleted as it disappears off the chart
                        frameRate: 30,    // data points are drawn 30 times every second
                        
                        // a callback to update datasets
                        onRefresh: chart => {
                            chart.data.datasets.forEach(dataset => {
                                    dataset.data.push({
                                    x: Date.now(),
                                    y: document.getElementById("pH").innerHTML
                                })
                            });
                        }
                        }
                    },
                }
            }
            });
    
        </script>
 
    </div>
    <div id="div6" style="float: right; width: 50%; height: 50%;">
        <div style="font-size: x-large; text-align: center;" id="text1">TSS</div>
        <canvas id="myChart6" style="width:100%;max-width:700px"></canvas> 
               <script>
            new Chart("myChart6", {
            type: "line",
            data: {
                datasets: [{
                    label: "mg/L",
                    backgroundColor: "rgba(0,0,0,1.0)",
                    borderColor: "rgba(0,0,0,0.1)",
                    data: []
                }]
            },
            options: {
                legend: {display: false},
                scales: {
                    x: {
                        type: 'realtime',   // x axis will auto-scroll from right to left
                        realtime: {         // per-axis options
                        duration: 20000,  // data in the past 20000 ms will be displayed
                        refresh: 1000,    // onRefresh callback will be called every 1000 ms
                        delay: 1000,      // delay of 1000 ms, so upcoming values are known before plotting a line
                        pause: false,     // chart is not paused
                        ttl: undefined,   // data will be automatically deleted as it disappears off the chart
                        frameRate: 30,    // data points are drawn 30 times every second
                        
                        // a callback to update datasets
                        onRefresh: chart => {
                            chart.data.datasets.forEach(dataset => {
                                    dataset.data.push({
                                    x: Date.now(),
                                    y: document.getElementById("TSS").innerHTML
                                })
                            });
                        }
                        }
                    },
                }
            }
            });
    
        </script>
 
    </div>
    <div id="div7" style="float: left; width: 50%; height: 50%;">
        <div style="font-size: x-large; text-align: center;" id="text1">COD</div>
        <canvas id="myChart7" style="width:100%;max-width:700px"></canvas> 
               <script>
            new Chart("myChart7", {
            type: "line",
            data: {
                datasets: [{
                    label: "mg/L",
                    backgroundColor: "rgba(0,0,0,1.0)",
                    borderColor: "rgba(0,0,0,0.1)",
                    data: []
                }]
            },
            options: {
                legend: {display: false},
                scales: {
                    x: {
                        type: 'realtime',   // x axis will auto-scroll from right to left
                        realtime: {         // per-axis options
                        duration: 20000,  // data in the past 20000 ms will be displayed
                        refresh: 1000,    // onRefresh callback will be called every 1000 ms
                        delay: 1000,      // delay of 1000 ms, so upcoming values are known before plotting a line
                        pause: false,     // chart is not paused
                        ttl: undefined,   // data will be automatically deleted as it disappears off the chart
                        frameRate: 30,    // data points are drawn 30 times every second
                        
                        // a callback to update datasets
                        onRefresh: chart => {
                            chart.data.datasets.forEach(dataset => {
                                    dataset.data.push({
                                    x: Date.now(),
                                    y: document.getElementById("COD").innerHTML
                                })
                            });
                        }
                        }
                    },
                }
            }
            });
    
        </script>
 
    </div>
    <div id="div8" style="float: left; width: 50%; height: 50%;">
        <div style="font-size: x-large; text-align: center;" id="text1">NH4+</div>
        <canvas id="myChart8" style="width:100%;max-width:700px"></canvas> 
               <script>
            new Chart("myChart8", {
            type: "line",
            data: {
                datasets: [{
                    label: "mg/L",
                    backgroundColor: "rgba(0,0,0,1.0)",
                    borderColor: "rgba(0,0,0,0.1)",
                    data: []
                }]
            },
            options: {
                legend: {display: false},
                scales: {
                    x: {
                        type: 'realtime',   // x axis will auto-scroll from right to left
                        realtime: {         // per-axis options
                        duration: 20000,  // data in the past 20000 ms will be displayed
                        refresh: 1000,    // onRefresh callback will be called every 1000 ms
                        delay: 1000,      // delay of 1000 ms, so upcoming values are known before plotting a line
                        pause: false,     // chart is not paused
                        ttl: undefined,   // data will be automatically deleted as it disappears off the chart
                        frameRate: 30,    // data points are drawn 30 times every second
                        
                        // a callback to update datasets
                        onRefresh: chart => {
                            chart.data.datasets.forEach(dataset => {
                                    dataset.data.push({
                                    x: Date.now(),
                                    y: document.getElementById("NH4+").innerHTML
                                })
                            });
                        }
                        }
                    },
                }
            }
            });
    
        </script>
 
    </div>

</div>
